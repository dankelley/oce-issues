library(oce)
if (!interactive()) png("757_%02d.png")
data(geosecs235, package="ocedata")
spans <- c(1e2, 2e2, 5e2, 1e3, 2e3, 5e3, 1e4, 2e4, 5e4, 1e5, 2e5, 5e5, 1e6)
for (span in spans)
    plot(geosecs235,span=span)
if (!interactive()) dev.off()
